import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  jobItemContainer: {
    marginVertical: 10,
  },
  dataContainer: {
    flexDirection: 'row',
  },
});

export default styles;
